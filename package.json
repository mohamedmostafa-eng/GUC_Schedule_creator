{
  "name": "guc-timetable-matrix",
  "version": "2.1.1",
  "private": true,
  "description": "Chrome MV3 extension: personalized GUC timetable matrix. Dev-dependencies here are for the offline test suite only — the extension itself needs no build tooling.",
  "scripts": {
    "test": "node tests/node-runner.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}
